// Set -> 1. unique elements - no two elements ina  set can be equal
// 2. immutable - we can add or remove elements from a set, but we can't change
// the values of existing element
//  sorted order - by default order is increasing but we have an option to
//  change that

#include <iostream>
#include <set>
using namespace std;

int main() {
  //for ascendnig
  // set<int> set1 = {3, 8, 1, 4, 9, 8};
  //for descending
  // set<int, greater<int>> set1 = {3, 8, 1, 4, 9, 8};

  // set1.insert(20);
  // set1.erase(1); // delte value from the set
  //set1.clear(); //delete all elements

  cout << set1.size();
  // for (int i : set1) {
  //   cout << i << " ";
  // }
}